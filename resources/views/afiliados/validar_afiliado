<form action="{{ route('afiliados.validar') }}" method="post">
    @csrf
    <label for="ci">Ingrese CI del Afiliado:</label>
    <input type="text" name="ci" required>
    <button type="submit">Validar Afiliado</button>
</form>